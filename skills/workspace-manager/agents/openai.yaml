interface:
  display_name: "Workspace Manager"
  short_description: "Map workspaces and govern project artifacts"
  default_prompt: "Use $workspace-manager to update workspace structure, artifact placement, retention, or authorized cleanup rules."
